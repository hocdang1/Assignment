extends ../layout

block content
  h1 Comments
  ul
    each comment in comments
      li
        | #{comment.content}
        if comment.author
          |  — by #{comment.author.name}
        |
        a(href=`/comments/${comment.id}/user`) Show user
