extends ../layout

block content
  h1 Comments by #{user.name}
  p: a(href=`/users/${user.id}`) back to user
  ul
    each comment in comments
      li
        | #{comment.content}
        if comment.post
          |  (on post: #{comment.post.content})
