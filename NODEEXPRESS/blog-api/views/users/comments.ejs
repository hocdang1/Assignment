<%- include('../partials/header') %>

<p>
  <a href="/users">← Back to user list</a>
  |
  <a href="/users/<%= user.id %>">User detail</a>
</p>

<% if (comments.length === 0) { %>
  <p class="empty">This user has no comments.</p>
<% } else { %>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Content</th>
        <th>On post</th>
      </tr>
    </thead>
    <tbody>
      <% comments.forEach(function (comment) { %>
        <tr>
          <td><%= comment.id %></td>
          <td><%= comment.content %></td>
          <td>
            <% if (comment.post) { %>
              #<%= comment.post.id %> — <%= comment.post.content %>
            <% } else { %>
              -
            <% } %>
          </td>
        </tr>
      <% }); %>
    </tbody>
  </table>
<% } %>

<%- include('../partials/footer') %>
